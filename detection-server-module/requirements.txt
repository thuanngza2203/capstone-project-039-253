# -*- coding: utf-8 -*-
# Cài một lệnh: pip install -r requirements.txt
# Đã chạy thử: Python 3.10.12, Linux, torch 2.14.0 CPU (xem CHANGELOG.md).
#
# PyTorch: dòng torch/torchvision bên dưới lấy bản mặc định của PyPI (Linux: CUDA,
# Windows/macOS: CPU). Muốn chọn bản cụ thể thì cài torch TRƯỚC, rồi mới cài file này:
#   CPU : pip install torch==2.14.0 torchvision==0.29.0 --index-url https://download.pytorch.org/whl/cpu
#   CUDA: chọn lệnh tại https://pytorch.org/get-started/locally/ (ví dụ .../whl/cu128)

# API
fastapi==0.141.1
uvicorn[standard]==0.53.0
python-multipart==0.0.32
pydantic==2.13.5
pydantic-settings==2.15.0
python-dotenv==1.2.3
httpx==0.28.1

# LLM normalizer/answer (Groq) + MongoDB
groq==1.7.0
pymongo==4.18.1
motor==3.7.1

# Nhận diện ảnh: ConvNeXt (timm), GenYOLO (ultralytics), IEViT (torch)
torch==2.14.0
torchvision==0.29.0
timm==1.0.30
ultralytics==8.4.160
pillow==12.3.0
requests==2.34.2
huggingface-hub==1.32.0

# Feedback RAG (local embedding + reranker) và rag/ nội bộ cho ANSWER_BACKEND=groq
sentence-transformers==5.7.0
numpy==2.2.6
langchain-core==1.6.4
langchain-chroma==1.1.0
langchain-huggingface==1.2.2
langchain-text-splitters==1.1.2
chromadb==1.5.9

# Test
pytest==9.1.1
